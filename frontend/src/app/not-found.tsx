import MainLayout from '@/components/layout';
import NotFound from '@/components/ui/blocks/NotFound';
import React from 'react';

export default function NotFoundPage() {
  return (
    <MainLayout>
      <NotFound />;
    </MainLayout>
  );
}
